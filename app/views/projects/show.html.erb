<h1><%= @project.title %></h1>

<p><%= link_to "Create", new_project_review_path(@project) %></p>
<p><%= link_to "Edit", edit_project_path(@project) %></p>
<p><%= link_to "Delete", project_path(@project),
                         :data => {:confirm => "You sure?",
                                   :method => "delete"} %></p>

<p><%= link_to "Home", projects_path %></p>
